logging:
    level:
        org.hibernate.SQL: debug

spring:
    #DB Connection
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3309/ks_board
        username: root
        password: 1234

    #JPA
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
        properties:
            hibernate:
                format_sql: true

